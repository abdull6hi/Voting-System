-- Create database
CREATE DATABASE IF NOT EXISTS university_voting;
USE university_voting;

-- Create users table
CREATE TABLE IF NOT EXISTS tbl_users (
    id INT AUTO_INCREMENT PRIMARY KEY,
    full_name VARCHAR(100) NOT NULL,
    admission_number VARCHAR(50) NOT NULL UNIQUE,
    email VARCHAR(100),
    password VARCHAR(255) NOT NULL,
    role VARCHAR(20) NOT NULL DEFAULT 'student'
);

-- Create votes table
CREATE TABLE IF NOT EXISTS tbl_votes (
    id INT AUTO_INCREMENT PRIMARY KEY,
    voter_admission_number VARCHAR(50) NOT NULL,
    candidate_admission_number VARCHAR(50) NOT NULL,
    vote_time TIMESTAMP DEFAULT CURRENT_TIMESTAMP,
    FOREIGN KEY (voter_admission_number) REFERENCES tbl_users(admission_number),
    FOREIGN KEY (candidate_admission_number) REFERENCES tbl_users(admission_number)
);

-- Insert admin user
INSERT INTO tbl_users (full_name, admission_number, email, password, role)
VALUES ('Admin User', 'admin001', 'admin@example.com', 'adminpassword', 'admin');
